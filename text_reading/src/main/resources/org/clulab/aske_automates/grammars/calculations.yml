vars: org/clulab/aske_automates/grammars/vars.yml

rules:

  ####CALCULATIONS####

  - name: var_based_on
    label: Calculation
    priority: ${priority}
    type: dependency
    example: "The average soil water potential ( S , J kg−1) is calculated based on a representative root length fraction for each soil layer (fr,j):"
    #action: variableArguments
    pattern: |
      trigger = [lemma = /calculate/]
      variable: Variable = nsubjpass appos
      conceptInvolved: Concept+ = advcl_based_on

